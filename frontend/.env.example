# API URL for production or preview builds. In local dev, Vite proxy handles /api automatically.
VITE_API_URL=https://task-management-w10t.onrender.com/api

# Socket.IO URL for production or preview builds. In local dev, Vite proxy handles socket connections automatically.
VITE_SOCKET_URL=https://task-management-w10t.onrender.com
